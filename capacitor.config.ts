import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sapo.app',
  appName: 'Sapoapp',
  webDir: 'dist/angular-18-primeng-app/browser'
};

export default config;
